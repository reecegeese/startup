1. What is the default port for HTTP/HTTPS/SSH?
   HTTP 80; HTTPS 443; SSH TCP 22
2. What does an HTTP status code in the range of 300/400/500 indicate?
   Redirection and client and service errors
3. What does the HTTP header content-type allow you to do?
   Say what kind of content is in the body of the message
4. What does a “Secure cookie”/”Http-only cookie”/”Same-site cookie” do?
   Secure-cookie only works through secure https
   Http-only blocks client side java script
   Same-site tells the site when to send cookies to prevent site request forgery 5. Assuming the following Express middleware, what would be the console.log output for an HTTP GET request with a URL path of /api/document?
5. What is the websocket protocol intended to provide?
   Two way communication over one tcp connection
6. What do the following acronyms stand for?
   JSX: Java script HML
   JS: JavaScript
   AWS:Amazon Web Service
   NPM: Node Package manager
   NVM: Node version manager
7. What does a React component with React.useState do?
   Gains memeory and becomes dynamic
8. What are React Hooks used for?
   Functions that hook into react state and lifecycle features. Don't work in classes
9. What does the State Hook(useState) do?
   Lets functions manage and preserve data acrossre-renders
10. What does the Context Hook do?
    Grabs user attention
11. What does the Ref Hook(useRef) do?
    Accsess and maipulates DOM and keeps mutable values across re-renders
12. What does the Effect Hook(useEffect) do?
    Sychronizes react companents with outside systems like fetching data or subscriptions
13. Performance Hook(useMemo or useCallback) do?
    Optimizes aplications by avoiding redundancy and memorizing results and functions
14. What does the fetch function do?
    requests resouces from a server like api's or databases
15. What does node.js do?
    Enables use of Javascript built in the backend
16. What does pm2 do?
    Process manager that keeps a apllication online and running 24/7
17. What does Vite do?
    Front end tool that shows live code updates
